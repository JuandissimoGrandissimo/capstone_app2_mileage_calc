# Optional distance lookup (choose ONE provider approach if you want):
# If not set, the app will use the miles you type manually.

# Example: OpenRouteService (recommended for students; free tier exists)
ORS_API_KEY=YOUR_KEY_HERE
